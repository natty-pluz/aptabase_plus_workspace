# Changelog

## 0.1.0

- Split the original Flutter-only SDK into pure Dart and Flutter packages.
- Added storage, system info, and HTTP transport abstractions.
- Moved Flutter plugin dependencies into `aptabase_flutter_plus`.
- Added Dart-only and Flutter examples inside each package.
- Added mirrored unit-test structure.
