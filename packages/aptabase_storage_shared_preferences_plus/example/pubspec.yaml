name: aptabase_storage_shared_preferences_plus_example
description: Flutter example for aptabase_storage_shared_preferences_plus.
publish_to: none

environment:
  sdk: '>=3.10.0 <4.0.0'
  flutter: '>=3.38.1'

resolution: workspace

dependencies:
  aptabase_flutter_plus:
    path: ../../aptabase_flutter_plus
  aptabase_storage_shared_preferences_plus:
    path: ..
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
