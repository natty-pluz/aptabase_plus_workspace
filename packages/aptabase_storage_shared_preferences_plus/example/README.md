# Shared Preferences Storage Example

This Flutter example shows how to initialise `aptabase_flutter_plus` with
`SharedPreferencesAptabaseStorage` from `aptabase_storage_shared_preferences_plus`.

```bash
flutter run --dart-define=APTABASE_APP_KEY=A-US-0000000000
```
