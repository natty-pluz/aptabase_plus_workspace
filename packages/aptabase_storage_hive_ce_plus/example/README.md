# Hive CE Storage Example

This Dart example shows how to use `HiveCeAptabaseStorage` with the pure Dart
`aptabase_plus` client.

```bash
dart run bin/main.dart --define=APTABASE_APP_KEY=A-US-0000000000
```

For normal Dart CLI usage, pass a directory path when opening the Hive storage.
