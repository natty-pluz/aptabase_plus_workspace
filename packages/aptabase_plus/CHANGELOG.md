# Changelog

## 0.1.0

- Initial pure Dart split from `aptabase_flutter`.
- Added `AptabaseClient`, `AptabaseStorage`, `AptabaseSystemInfoProvider`, and `AptabaseTransport`.
- Added default in-memory storage and HTTP transport.
